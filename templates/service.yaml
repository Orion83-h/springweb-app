apiVersion: v1
kind: Service
metadata:
  name: {{ include "springweb-app.fullname" . }}
  labels:
    {{- include "springweb-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "springweb-app.selectorLabels" . | nindent 4 }}
